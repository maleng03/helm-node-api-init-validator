apiVersion: v1
kind: Service
metadata:
  name: {{ include "node-api-app.fullname" . }}
  labels:
    {{- include "node-api-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "node-api-app.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      nodePort: {{ .Values.service.nodePort }}